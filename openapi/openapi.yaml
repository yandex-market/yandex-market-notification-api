openapi: 3.0.3
info:
  title: API Яндекс Маркета для продавцов (Push-нотификации)
  description: >
    Часть API Яндекс Маркета для продавцов, отвечающая за нотификацию внешнего
    API магазинов о событиях.
  version: LATEST
paths:
  /notification:
    $ref: paths/notification.yaml
