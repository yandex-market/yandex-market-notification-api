description: Количество единиц услуги в указанном статусе.
type: object
required:
  - status
  - count
properties:
  status:
    $ref: ./OrderLineServiceStatusType.yaml
  count:
    description: Количество единиц услуги в статусе.
    type: integer
    format: int64
