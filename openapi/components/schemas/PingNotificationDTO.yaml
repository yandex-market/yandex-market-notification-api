description: Проверочное уведомление (notificationType = PING).
type: object
properties:
  notificationType:
    $ref: ./NotificationType.yaml
  time:
    description: >
      Дата и время обработки уведомления со стороны магазина.


      Формат даты: ISO 8601 со смещением относительно UTC. Например,
      `2017-11-21T00:00:00.213Z`.
    type: string
    format: date-time
