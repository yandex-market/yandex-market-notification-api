description: "Уведомление об изменении статуса заказа.\n\n`notificationType` = `ORDER_STATUS_UPDATED`\n\n{% note tip \"Чтобы изменить статус заказа\" %}\n\nИспользуйте метод [PUT\_v2/campaigns/{campaignId}/orders/{orderId}/status](../../reference/orders/updateOrderStatus.md).\n\n{% endnote %}\n"
type: object
required:
  - notificationType
  - orderId
  - campaignId
  - status
  - substatus
  - updatedAt
properties:
  notificationType:
    $ref: ./NotificationType.yaml
  orderId:
    description: Идентификатор заказа.
    type: integer
    format: int64
  campaignId:
    $ref: ./CampaignId.yaml
  status:
    $ref: ./OrderStatusType.yaml
  substatus:
    $ref: ./OrderSubstatusType.yaml
  updatedAt:
    description: >
      Дата и время изменения статуса заказа.


      Формат даты: ISO 8601 со смещением относительно UTC. Например,
      `2017-11-21T00:00:00.213Z`.
    type: string
    format: date-time
